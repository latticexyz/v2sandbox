export enum Scenes {
  Main = "Main",
}

export enum Maps {
  Main = "Main",
}

export enum Animations {}
export enum Sprites {}
export enum Assets {}

export const TILE_HEIGHT = 32;
export const TILE_WIDTH = 32;